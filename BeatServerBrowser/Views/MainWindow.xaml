<Window x:Class="BeatServerBrowser.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:action="clr-namespace:BeatServerBrowser.Core.Actions;assembly=BeatServerBrowser.Core"
        xmlns:prism="http://prismlibrary.com/"
        WindowStartupLocation="CenterScreen"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Background="{DynamicResource MaterialDesignBackground}"
        Title="{Binding Title}" Height="720" Width="1349" >
    <b:Interaction.Triggers>
        <b:EventTrigger x:Name="Loaded">
            <action:DataContextInitialize/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <ContentControl prism:RegionManager.RegionName="ContentRegion" />
    </Grid>
</Window>
