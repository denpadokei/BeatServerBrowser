<UserControl x:Class="BeatServerBrowser.Home.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:usercontrol="clr-namespace:BeatServerBrowser.Home.UserControls"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:control="clr-namespace:BeatServerBrowser.Style.Controls;assembly=BeatServerBrowser.Style"
             xmlns:viewmodel="clr-namespace:BeatServerBrowser.Home.ViewModels"
             xmlns:enum="clr-namespace:BeatServerBrowser.Static.Enums;assembly=BeatServerBrowser.Static"
             xmlns:extention="clr-namespace:BeatServerBrowser.Core.Extentions;assembly=BeatServerBrowser.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1200" d:DesignHeight="800"
             d:DataContext="{d:DesignInstance {x:Type viewmodel:HomeViewModel}, IsDesignTimeCreatable=True}"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodel:BeatMapViewModel}">
            <usercontrol:BeatMapView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="設定" Command="{Binding ShowSettingWindowCommand}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20" Cursor="Hand"/>
        <control:ListNavigateControl Grid.Row="1" CurrentListViewContext="{Binding CurrentListViewContext}">
            <TabItem Name="Panel" Header="リスト形式">
                <ContentControl prism:RegionManager.RegionName="PanelList"/>
            </TabItem>
            <TabItem Name="Grid" Header="グリッド形式" >
                <ContentControl prism:RegionManager.RegionName="GridList"/>
            </TabItem>
        </control:ListNavigateControl>
    </Grid>
</UserControl>
